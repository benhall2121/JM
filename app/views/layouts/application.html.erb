<!DOCTYPE html>
<html>
<head>
  <title>JM</title>
  <link href='http://fonts.googleapis.com/css?family=Cabin' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.4.2/jquery.min.js"></script>
</head>
<body>

<div class="all">
    <% if current_user %>
    <div id="firstNav">
        <div id="history_count" class="history_count"><%= current_user.histories.count %></div>
    	<div class="home_header"><%= link_to "Home", user_path(current_user.id) %></div> 
    	<div class="about_header"><%= link_to "About", page_path(1) %></div>
    </div>
    <div id="secondNav">
    	<%= link_to "Create", new_commentary_path %> | 
    	<%= link_to "Boost", commentaries_path %> | 
    	<%= link_to "Share", shareable_commentaries_path %>
    </div>
    
    <% end %>

<%= yield %>
</div>

<div class="footer">
    <% if current_user %>
      <%= link_to "Edit Profile", edit_user_path(current_user.id)%> |
      <%= link_to "Logout", :logout%>
   <% end %>
</div>
</body>
</html>
