<%= form_for(@site_guide) do |f| %>
  <% if @site_guide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_guide.errors.count, "error") %> prohibited this site_guide from being saved:</h2>

      <ul>
      <% @site_guide.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br />
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :answer %><br />
    <%= f.text_field :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
