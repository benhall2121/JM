<%= render 'shared/about_nav' %>
<div class="about_sections_main_div">
<div class="about_sections_header">About <% if logged_in? && is_admin? && @abouts.length == 0 %><span class="new_site_guide_faq"><%= link_to 'New About', new_about_path %></span><% end %></div>

<% @abouts.each do |about| %>
 <div class="about_wrapper">
  <div><%= about.body %></div>
  <% if logged_in? && is_admin? %><div class="about_admin_div"><%= link_to 'Edit', edit_about_path(about) %> <%= link_to 'Delete', about, :confirm => 'Are you sure?', :method => :delete %></div><% end %>
 </div>
<% end %>
</div>
